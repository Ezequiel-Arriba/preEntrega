# pre-entrega_Java_facil

EL programa consiste de una gstos de stock simples, onde agregamo objetos (ID,Nombre,Precio,Stock).

el id es agregado por el sistema no el usuario.

podemos listar los productos, eliminar por id y modificar por id.